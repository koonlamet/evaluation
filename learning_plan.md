# แผนการสอนเตรียมแข่งขัน (Learning Plan) — ฉบับเริ่มจากศูนย์

**เป้าหมาย:** นักเรียน ปวช.2 จำนวน 3 คน **ไม่มีพื้นฐานเขียนโปรแกรมเลย** → สร้างระบบเว็บ (front + back + database) จาก template ที่กรรมการให้ ได้ทันภายใน 5 ชั่วโมง
**เวลาสอน:** สัปดาห์ละ 3 ชั่วโมง รวม **19 สัปดาห์ (57 ชม.)**
**เครื่องมือประกอบ:** ระบบ `pes-app` ที่เสร็จแล้ว + เอกสาร 4 ตัว ([TREE.md](TREE.md), [STRUCTURE.md](STRUCTURE.md), [apidoc.md](apidoc.md), [frontend.md](frontend.md)) + template จริงจากกรรมการ (โฟลเดอร์ `/PES`)

**การแบ่งบทบาท (ใช้ตั้งแต่ Phase 2):**

| คน | บทบาท | รับผิดชอบตอนแข่ง |
|---|---|---|
| คนที่ 1 | Frontend | หน้าเว็บทั้งหมด (Nuxt + Vuetify) |
| คนที่ 2 | Backend | API ทั้งหมด (Express + Knex) |
| คนที่ 3 | Database + Docs | schema, seed data, เอกสาร API, UX/UI, ทดสอบระบบ |

---

## ภาพรวม 5 Phase

```
Phase 0 (สัปดาห์ 1-5)    ปูพื้นฐาน JS/HTML/SQL      ← ทั้ง 3 คนเรียนเหมือนกัน
Phase 1 (สัปดาห์ 6-9)    เข้าใจภาพรวมระบบ           ← ทั้ง 3 คนเรียนเหมือนกัน
Phase 2 (สัปดาห์ 10-13)  ฝึก pattern + แยกสาย        ← เริ่มแยกตามบทบาท
Phase 3 (สัปดาห์ 14-16)  งานยาก + ทำงานเป็นทีม       ← แยกสาย + ประกอบร่าง
Phase 4 (สัปดาห์ 17-19)  ซ้อมแข่งจับเวลา              ← จำลองวันแข่งจริง
```

**หลักการสอนทั้งแผน (สำคัญมากสำหรับเด็กไม่มีพื้นฐาน):**
1. **ทุกแบบฝึกใช้ของจริงจาก pes-app** — ไม่ใช้โจทย์ลอยๆ อย่าง "จงเขียนโปรแกรมคำนวณเกรด" เด็กฝึก SELECT กับตาราง `users` จริง ฝึกอ่าน object ที่เป็น response จริงของ API
2. **เห็นของจริงทำงานก่อน → แกะว่าทำงานยังไง → ลอกดัดแปลง → (ถ้าไหว) เขียนเอง** — สำหรับเด็กเริ่มจากศูนย์ เป้าหมายสูงสุดคือ "ลอก pattern แล้วดัดแปลงถูกจุด" ซึ่งพอสำหรับการแข่ง เพราะโจทย์ ~70% คือ CRUD ที่ลอกได้
3. ทฤษฎีสอนเฉพาะที่จำเป็น ทุกอย่างที่เรียนต้องตอบได้ว่า "อยู่ตรงไหนในเว็บที่เห็น"
4. ความเร็วสำคัญกว่าความกว้าง — ตอนแข่งมี 5 ชม.

---

## Phase 0: ปูพื้นฐาน (สัปดาห์ 1-5) — เรียนร่วมกันทั้ง 3 คน

> เป้าหมาย: อ่านโค้ด JS สั้นๆ ออก, รู้ว่า tag/attribute คืออะไร, ใช้ SQL พื้นฐาน 4 คำสั่งได้
> **ไม่ใช่** คอร์ส HTML/JS/SQL เต็มรูปแบบ — สอนเฉพาะชุดเล็กที่สุดที่ต้องใช้อ่านโค้ดโปรเจกต์นี้

### สัปดาห์ 1 — JS เริ่มต้น: ตัวแปร + ฟังก์ชัน (ฝึกใน browser console ไม่ต้องติดตั้งอะไร)
| ชม. | กิจกรรม |
|---|---|
| 1 | เปิดเว็บ pes-app ให้ดูก่อนว่า "ปลายทางที่เรากำลังจะไป" หน้าตาเป็นแบบนี้ แล้วเปิด DevTools Console อธิบายว่านี่คือที่ทดลองภาษา JS |
| 2 | ตัวแปร (`let`, `const`), ชนิดข้อมูล (ตัวเลข, ข้อความ, boolean), การคำนวณ + ต่อข้อความ — พิมพ์ตามใน console ทีละบรรทัด |
| 3 | ฟังก์ชัน: ประกาศ รับค่า คืนค่า — เริ่มจาก `function bmi(w,h){...}` แล้วชี้ให้ดูว่าในโปรเจกต์จริงก็มีหน้าตาแบบนี้ (เปิด `weightedAverage` ใน `report.controller.js` ให้ดูเฉยๆ ยังไม่ต้องเข้าใจ) |

**การบ้าน:** เขียนฟังก์ชันเล็ก 3 ตัวใน console (เช่น คิดเงินรวม, ตัดเกรด) ถ่ายรูปผลลัพธ์มาส่ง

### สัปดาห์ 2 — JS ต่อ: object / array / เงื่อนไข / ลูป (หัวใจของการอ่านโค้ดทั้งโปรเจกต์)
| ชม. | กิจกรรม |
|---|---|
| 1 | object `{}` และ array `[]` — **ใช้ข้อมูลจริง:** เปิด DevTools Network ตอน login ที่ pes-app แล้ว copy response `{success, accessToken, user:{...}}` มาแกะใน console: `data.user.name` คืออะไร |
| 2 | `if/else` และ `for`/`.map()`/`.filter()` — โจทย์: จาก array รายชื่อ users จริง (copy จาก API `/api/users`) ให้กรองเฉพาะ role "evaluatee" |
| 3 | JSON คืออะไร (แค่ "object ที่ส่งข้ามเครื่องได้") + แบบฝึกรวม: อ่าน response จริงของ `/api/summary` แล้วตอบคำถามครู ("คะแนนเฉลี่ยอยู่ field ไหน") |

**การบ้าน:** ครูให้ JSON จริงจาก apidoc.md 1 ก้อน + คำถาม 5 ข้อ ให้เขียนโค้ดดึงคำตอบ

### สัปดาห์ 3 — HTML + แนวคิด tag/attribute → โยงเข้า Vuetify
| ชม. | กิจกรรม |
|---|---|
| 1 | HTML คืออะไร: tag, attribute, การซ้อนกัน — เขียนไฟล์ `.html` เปล่าๆ 1 ไฟล์ (หัวข้อ, ปุ่ม, ตาราง, รูป) เปิดใน browser |
| 2 | เปิด DevTools → Elements ที่หน้า pes-app จริง ชี้ให้เห็นว่า "ปุ่มสวยๆ ที่เห็น ก็คือ tag ซ้อนกัน" แล้วเปิด [frontend.md](frontend.md) แนะนำว่านี่คือพจนานุกรม tag ทั้งหมดที่จะใช้ |
| 3 | แบบฝึก: ครูชี้ element บนหน้าเว็บจริง (ปุ่ม, ตาราง, การ์ด) ให้เด็กหาใน frontend.md ว่าคือ tag อะไร attribute ไหนคุมสี/ขนาด |

**การบ้าน:** เขียนหน้า HTML แนะนำตัวเอง 1 หน้า (ใช้ tag อย่างน้อย 6 ชนิด)

### สัปดาห์ 4 — SQL พื้นฐาน 4 คำสั่ง (ฝึกกับฐานข้อมูลจริงของ pes-app ผ่าน phpMyAdmin)
| ชม. | กิจกรรม |
|---|---|
| 1 | ฐานข้อมูลคืออะไร: ตาราง แถว คอลัมน์ — เปิด phpMyAdmin ดูตาราง `users` จริง แล้วสอน `SELECT * FROM users` → `WHERE role='evaluatee'` → `ORDER BY` |
| 2 | `INSERT` (เพิ่มผู้ใช้ปลอม 1 คน), `UPDATE` (แก้ชื่อ), `DELETE` (ลบทิ้ง) — ทำในตารางจริง เห็นผลบนหน้าเว็บจริงทันที (refresh หน้า admin/users) — **จุดเชื่อมสำคัญ: "แก้ db แล้วเว็บเปลี่ยน" คือครั้งแรกที่เด็กเห็นว่า 3 กล่องต่อกันจริง** |
| 3 | แบบฝึกโจทย์ 10 ข้อกับตารางจริง (หาคน, นับจำนวน `COUNT`, แก้ค่า) |

**การบ้าน:** โจทย์ SQL 10 ข้อ ทำใน phpMyAdmin ถ่ายรูปผล

### สัปดาห์ 5 — FK + JOIN เบื้องต้น + async/await ผิวๆ + สอบวัดผล Phase 0
| ชม. | กิจกรรม |
|---|---|
| 1 | foreign key คืออะไร: ดู `assignments` ที่เก็บ `evaluator_id` ชี้ไป `users.id` → สอน `JOIN` แบบง่ายสุด 1 รูปแบบ (เอาชื่อคนมาแทนตัวเลข id) |
| 2 | `async/await` แบบผิวที่สุด — แค่ให้รู้ว่า "โค้ดที่ต้องรอ server จะมีคำว่า await นำหน้า" ลอง `await fetch('/api/users')` ใน console ที่หน้า pes-app จริง (login แล้ว) ให้เห็นข้อมูลไหลกลับมา |
| 3 | **สอบวัดผล Phase 0** (รายคน): อ่านโค้ด JS 10 บรรทัดแล้วบอกผลลัพธ์ / เขียน SELECT+WHERE เอง / ชี้ tag บนหน้าเว็บแล้วบอกชื่อได้ |

**✅ เกณฑ์ผ่าน Phase 0 (ต้องผ่านทุกคนก่อนขึ้น Phase 1):**
- อ่านฟังก์ชัน JS สั้นๆ ที่มี if/loop/object แล้วบอกได้ว่าทำอะไร
- เขียน `SELECT ... WHERE` และ `INSERT` เองได้กับตารางที่กำหนด
- เห็น JSON แล้วดึงค่าที่ต้องการออกมาได้
- ถ้าไม่ผ่าน: เพิ่มสัปดาห์ซ่อมได้ 1 สัปดาห์ — **อย่าลากเกิน 6 สัปดาห์** เพราะพื้นฐานที่เหลือจะไปแน่นเองตอนใช้จริงใน Phase ถัดไป

---

## Phase 1: เข้าใจภาพรวมทั้งระบบ (สัปดาห์ 6-9) — เรียนร่วมกันทั้ง 3 คน

> เป้าหมาย: อธิบายได้ว่าข้อมูลวิ่ง front → back → db → กลับมา ยังไง โดยไม่ต้องเปิดโค้ด

### สัปดาห์ 6 — เห็นภาพรวมจากของจริง
| ชม. | กิจกรรม |
|---|---|
| 1 | ครูรัน `docker compose up` สด วาดภาพ 3 กล่อง (frontend:3000 / backend:7000 / db:3306) อธิบายว่าใครคุยกับใคร — คราวนี้เด็กมีพื้นแล้ว จะเข้าใจคำว่า "ส่ง JSON" จริงๆ |
| 2-3 | เด็กเล่นเว็บครบ 3 บทบาท พร้อมเปิด DevTools → Network ค้างไว้ จดตาราง "ปุ่มที่กด → method+URL ที่วิ่ง" คนละ 10 คู่ |

### สัปดาห์ 7 — แผนที่ไฟล์ + ฐานข้อมูล
| ชม. | กิจกรรม |
|---|---|
| 1 | อ่าน [TREE.md](TREE.md) + [STRUCTURE.md](STRUCTURE.md) ข้อ 1-3 ร่วมกัน ให้เด็กวาดโครงโฟลเดอร์เองบนกระดาษ |
| 2 | วาด ER diagram 8 ตารางจาก phpMyAdmin — คราวนี้เด็กรู้จัก FK แล้ว ให้หาความสัมพันธ์เองทั้งหมด |
| 3 | เกมจับคู่: action บนเว็บ → request → ตารางที่กระทบ |

### สัปดาห์ 8 — ไล่เส้นทางที่ 1 แบบเต็ม: login (สำคัญที่สุดของ Phase 1)
| ชม. | กิจกรรม |
|---|---|
| 1-2 | ไล่ตาม STRUCTURE.md ข้อ 6 ทีละไฟล์: `login.vue` → `useApi.js` → `auth.routes.js` → `auth.controller.js` → ตาราง `users` → token เก็บ localStorage → `auth.global.js` — ใช้ comment ในไฟล์เป็นตัวช่วย ให้เด็กอ่านแล้วเล่าให้เพื่อนฟัง (peer-explain) สลับกันทีละท่อน |
| 3 | ปิดโค้ด → แต่ละคนวาด diagram เส้นทาง login จากความจำ |

### สัปดาห์ 9 — เส้นทางที่ 2 (เขียนข้อมูล+ไฟล์แนบ) + สอบวัดผล Phase 1
| ชม. | กิจกรรม |
|---|---|
| 1-2 | ไล่เส้นทาง "เขียนข้อมูล + อัปโหลดไฟล์": `me/period/[id].vue` → `evidences.routes.js` → `upload.js` → `evidences.controller.js` → ตาราง `evidences` — ชี้ว่าโครงเดิมกับ login แค่เปลี่ยนปลายทาง |
| 3 | **สอบวัดผล:** ครูพูด action สุ่ม เด็กไล่หาไฟล์ที่เกี่ยวข้องจาก TREE.md ภายใน 10 นาที ทำ 3 รอบ |

**✅ เกณฑ์ผ่าน Phase 1:**
- อธิบายเส้นทางข้อมูล 2 แบบ (อ่าน 1 + เขียน 1) ครบวงจรโดยไม่เปิดโค้ด
- หาไฟล์ถูกจาก action สุ่ม ภายในเวลาจำกัด

---

## Phase 2: ฝึก pattern ใช้ซ้ำ + เริ่มแยกสาย (สัปดาห์ 10-13)

> เป้าหมาย: เปลี่ยนจาก "อ่านออก" เป็น "ลอกดัดแปลงได้ถูกจุด" — สำหรับเด็กเริ่มจากศูนย์ นี่คือทักษะหลักของการแข่ง ไม่ใช่การเขียนสดจากไฟล์ว่าง

### สัปดาห์ 10 — pattern ฝั่ง backend: crud.js (เรียนร่วมกัน)
| ชม. | กิจกรรม |
|---|---|
| 1 | แกะ `utils/crud.js` + `utils/mountCrud.js` ทีละบรรทัด (เด็กอ่าน JS ออกแล้ว) — เข้าใจว่า "เขียนครั้งเดียว ได้ CRUD ทุกตาราง" |
| 2-3 | **ปฏิบัติ:** ทุกคนเพิ่ม resource ใหม่ 1 ตัว (เช่น `departments`): สร้างตารางใน phpMyAdmin + เพิ่ม `mountCrud` 5 บรรทัดใน `app.js` → ทดสอบเรียกผ่าน `await fetch(...)` ใน console |

### สัปดาห์ 11 — pattern ฝั่ง frontend: CrudTable.vue (เรียนร่วมกัน)
| ชม. | กิจกรรม |
|---|---|
| 1 | แกะ `components/CrudTable.vue` — props แต่ละตัว (endpoint, headers, fields) คุมอะไร ใช้ frontend.md ประกอบ |
| 2-3 | **ปฏิบัติ:** copy `admin/users.vue` มาเปลี่ยน endpoint/headers/fields ให้กลายเป็นหน้าจัดการ resource จากสัปดาห์ 10 → ได้หน้า CRUD ที่กดใช้ได้จริง |

**milestone สำคัญ:** ทุกคนได้ทำ "เพิ่ม 1 resource ครบ front+back" ด้วยตัวเอง 1 รอบ

### สัปดาห์ 12-13 — แยกสายครั้งแรก (คาบละ 2.5 ชม. แยก + 0.5 ชม. เล่าให้เพื่อนฟัง)
| คน | สิ่งที่ฝึก (เน้นลอก-ดัดแปลง ไม่ใช่เขียนสด) |
|---|---|
| Frontend | ดัดแปลง `login.vue`/`account.vue`/`admin/users.vue` ตามโจทย์ครู (เปลี่ยน field, เพิ่มช่อง, เปลี่ยน validation) โดยเปิดต้นฉบับ+frontend.md ได้ / ฝึกจนรู้ว่าจะแก้ตรงไหนโดยไม่ต้องไล่หา |
| Backend | ดัดแปลง `auth.controller.js`/`users.controller.js` ตามโจทย์ (เพิ่ม field ที่รับ, เปลี่ยนเงื่อนไข validate, เปลี่ยน status code) / ฝึก middleware `auth("role")` ว่าใส่ตรงไหนได้ผลยังไง |
| Database+Docs | เขียน CREATE TABLE + FK + seed จากโจทย์ใหม่ที่ไม่เคยเห็น / ฝึกแปลงโจทย์ภาษาไทย → รายการตาราง+field / เริ่มทำ API contract หน้าเดียว (แบบ gotchas) จาก apidoc.md |

**✅ เกณฑ์ผ่าน Phase 2:**
- ทุกคน: เพิ่ม resource CRUD ใหม่ครบวงจร (ลอก pattern) ได้ใน ~45 นาที
- Frontend: รับโจทย์ "เพิ่มช่องกรอก X ในหน้า Y" แล้วแก้ถูกจุดใน ~15 นาที
- Backend: รับโจทย์ "เพิ่ม field X ให้ API Y" แล้วแก้ครบ (route+controller) ใน ~15 นาที
- Database: แปลงโจทย์ระบบเล็กๆ เป็น schema 3-4 ตารางพร้อม FK ได้เอง

---

## Phase 3: งานยาก + ประกอบร่างเป็นทีม (สัปดาห์ 14-16)

### สัปดาห์ 14 — เจาะงานยากตามสาย
| คน | สิ่งที่ฝึก |
|---|---|
| Frontend | หน้าให้คะแนน `eval/[id].vue` (ฟอร์มซับซ้อน + computed), หน้าแนบไฟล์ `me/period/[id].vue` (multipart) |
| Backend | อัปโหลดไฟล์ (`upload.js` + multer), คำนวณคะแนนถ่วงน้ำหนัก (`report.controller.js`), LEFT JOIN สำหรับรายงาน |
| Database+Docs | JOIN หลายตาราง, ฝึกทำ API contract หน้าเดียวให้เร็วขึ้น, วางแผนการทดสอบ (test ทีละ flow) |

### สัปดาห์ 15 — Cross-review + โจทย์ integration จิ๋ว
| ชม. | กิจกรรม |
|---|---|
| 1 | สลับอ่านโค้ดข้ามสาย (Frontend อ่าน backend, Backend อ่าน schema, Database อ่านหน้า Vue) — เป้าหมาย "อ่านออก" พอ debug ร่วมกันได้ |
| 2-3 | **โจทย์ integration จิ๋ว:** feature ใหม่ 1 อัน (เช่น "เพิ่มช่องหมายเหตุตอนกรรมการส่งคะแนน") ทั้ง 3 คนแก้ส่วนตัวเอง (db เพิ่มคอลัมน์ → back รับ field → front เพิ่มช่อง) ให้จบใน 1.5 ชม. |

### สัปดาห์ 16 — สร้างระบบเล็กจากศูนย์ทั้งทีม
**โจทย์:** ระบบใหม่ ~3 ตาราง (เช่น ยืม-คืนอุปกรณ์) ทำจาก template `/PES` จริง เวลา 3 ชม. เต็ม
- บังคับขั้นตอนเหมือนวันแข่ง: 20 นาทีแรก**ตกลง API contract ร่วมกัน** (ตาราง, endpoint, ชื่อ field — กระดาษแผ่นเดียว) แล้วแยกไปทำ
- ครูสังเกตอย่างเดียว จดว่าติดตรงไหน เสียเวลาช่วงไหน

**✅ เกณฑ์ผ่าน Phase 3:** ระบบเล็กใช้งานได้จริงครบ flow (แม้ UI ไม่สวย) + integration ไม่เกิน 30 นาทีสุดท้าย

---

## Phase 4: ซ้อมแข่งจับเวลา (สัปดาห์ 17-19)

### เตรียมก่อนซ้อม (ครูทำ)
- เคลียร์ template `/PES`: ลบไฟล์ซ้ำ (login 4 เวอร์ชัน, daisyui, tailwind), ลบ console.log ใน `middlewares/auth.js` + `db/knex.js`, ลบ dead code (`requireAuth.js`, `utils/authz.js`)
- ทำ checklist "ไฟล์ไหนใน template ใช้ได้เลย / ต้องแก้ / ต้องเขียนเอง"
- เตรียมโจทย์ซ้อม 2-3 ชุด ขนาดใกล้โจทย์จริง (ระบบ ~5-8 ตาราง, auth + CRUD 3-4 resource + อัปโหลดไฟล์ + รายงาน 1 หน้า)

### สัปดาห์ 17 — ซ้อมครึ่งเกม (3 ชม.)
- โจทย์ชุดที่ 1: ทำให้ถึงจุด "contract เสร็จ + CRUD หลักเสร็จ" ใน 3 ชม.
- Debrief 15 นาที: อะไรช้ากว่าแผน เพราะอะไร

### สัปดาห์ 18 — ซ้อมเต็มเกม 5 ชม. (นัดพิเศษ เสาร์/อาทิตย์ ถ้าจัดได้)
- โจทย์ชุดที่ 2 จับเวลา 5 ชม. เต็ม ครูเป็นกรรมการ ไม่ช่วยเลย
- วัดผล: เสร็จกี่ % / integration ใช้เวลาเท่าไหร่ / จุดไหนหลุดเวลา
- **เสร็จใน 4 ชม. = ปลอดภัย / เสร็จพอดี 5 ชม. = เสี่ยง ต้องหาจุดเร่ง**

### สัปดาห์ 19 — อุดรอยรั่ว + ซ้อมรอบสุดท้าย
- ซ่อมจุดอ่อนจากสัปดาห์ 18 เฉพาะจุด
- ซ้อมสั้นด้วยโจทย์ชุดที่ 3 เฉพาะช่วงที่เคยหลุดเวลา (เช่น 90 นาทีแรก: ตั้งโปรเจกต์+contract+CRUD แรก)
- ทบทวน checklist วันแข่ง: ลำดับงานรายชั่วโมง ใครทำอะไรก่อน-หลัง จุดนัด integration

---

## ตารางเวลาแนะนำในวันแข่งจริง (5 ชม.)

| เวลา | Frontend | Backend | Database+Docs |
|---|---|---|---|
| 0:00-0:30 | **ทั้งทีมอ่านโจทย์ + ตกลง API contract ร่วมกัน** (ตาราง, endpoint, ชื่อ field — กระดาษ 1 แผ่น) |||
| 0:30-1:00 | ตั้งโปรเจกต์ + layout + login page | ตั้งโปรเจกต์ + db connect + auth | เขียน schema.sql + seed + รัน db |
| 1:00-2:30 | หน้า CRUD ทีละ resource (ลอก pattern CrudTable) | mountCrud ทีละ resource + endpoint พิเศษ | เอกสาร + ทดสอบ API ที่เสร็จทีละตัว + UX/UI |
| 2:30-3:30 | หน้า feature เฉพาะ (ฟอร์มซับซ้อน/อัปโหลด) | logic เฉพาะ (คำนวณ/รายงาน/ไฟล์) | ทดสอบ flow จริงเหมือนผู้ใช้ + จดบั๊ก |
| 3:30-4:30 | **Integration — ทั้งทีมต่อระบบเข้าด้วยกัน ไล่บั๊กตามรายการที่คนที่ 3 จด** |||
| 4:30-5:00 | เก็บรายละเอียด UI + เตรียมนำเสนอ + สำรองข้อมูล **ห้ามเริ่ม feature ใหม่หลัง 4:30** |||

---

## ถ้าเวลาไม่พอ 19 สัปดาห์ — ลำดับการตัด

1. **ห้ามตัด:** Phase 0 (ไม่มีพื้นคือต่อไม่ได้เลย) และ Phase 4 (การซ้อมจับเวลาชี้ขาดวันจริงที่สุด)
2. ตัดได้ก่อน: สัปดาห์ 14 (งานยากตามสาย) — สอนเฉพาะส่วนที่โจทย์แข่งเคยออก (อัปโหลดไฟล์มาแน่ ให้เก็บไว้ / รายงานซับซ้อนตัดได้)
3. ตัดได้ถัดมา: รวมสัปดาห์ 12-13 เหลือสัปดาห์เดียว (ลดจำนวนโจทย์ดัดแปลงต่อคน)
4. ทางลัดสุดท้ายถ้าบีบมาก: รวม Phase 1 สัปดาห์ 8-9 เหลือสัปดาห์เดียว (ไล่เส้นทาง login เส้นเดียว ตัดเส้นทางที่ 2) — เสี่ยงขึ้นแต่ยังไปต่อได้

## หลักที่ต้องย้ำกับเด็กตลอดแผน

1. **Contract ก่อน โค้ดทีหลัง** — 20-30 นาทีแรกที่ตกลงชื่อ field ร่วมกัน ประหยัดเวลา integration เป็นชั่วโมง
2. **Pattern เดิมซ้ำให้เร็ว** — งาน 70% ของโจทย์คือ CRUD ที่ฝึกมาแล้ว อย่าคิดใหม่หน้างาน
3. **เสร็จหยาบทั้ง flow ดีกว่าสวยครึ่งเดียว** — กรรมการให้คะแนนระบบที่ใช้ได้ครบ ไม่ใช่หน้า login ที่สวยสุด
4. **ห้ามเริ่มของใหม่ครึ่งชั่วโมงสุดท้าย** — เก็บกวาด ทดสอบ เตรียมนำเสนอเท่านั้น
5. **ติดเกิน 15 นาที ให้บอกเพื่อน** — ปัญหาที่คนเดียวแก้ไม่ออก อีกสองคนมักเห็นทางออกเร็วกว่า
